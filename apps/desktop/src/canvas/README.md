# Canvas

Owned by the Canvas team — see `../contracts/canvas.ts` for the public surface.

This directory will host the Excalidraw integration that implements
`CanvasController`. Empty in the Foundation segment.
