# Storage

Owned by the Storage team — see `../contracts/storage.ts` for the public surface.

This directory will host the SQLite + filesystem implementation of
`StorageAdapter`. Empty in the Foundation segment.
