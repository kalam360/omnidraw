# Agent

Owned by the Agent team — see `../contracts/` for the public surface.

This directory implements the `ChatModelAdapter` (and partial `AuthAdapter`)
contract. The Foundation segment leaves it empty intentionally: nothing here
yet, only the contract types in `src/contracts/chat.ts` and
`src/contracts/auth.ts`.
